import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'court.app',
  appName: 'SportCourtApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
